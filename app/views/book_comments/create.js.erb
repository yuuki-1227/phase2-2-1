$('#comment-index').html("<%= j (render 'book_comments/book_comment', book: @book) %>");
$('#comment-count').html("<%= j (render 'book_comments/comment_count', book: @book) %>");
$("textarea").val("");